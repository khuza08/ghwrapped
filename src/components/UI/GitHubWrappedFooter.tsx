import React from "react";

const GitHubWrappedFooter: React.FC = () => {
  return (
    <footer className="mt-8 sm:mt-12 text-center text-white/80 text-xs sm:text-sm">
      <p>
        yet another simple GitHub Wrapped by elza; This project uses only public
        GitHub data.
      </p>
    </footer>
  );
};

export default GitHubWrappedFooter;
